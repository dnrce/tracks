<%==
{
  type: "bar",
  data: {
    labels: t('date.day_names'),
    datasets: [
      {
        label: t("stats.labels.created"),
        data: @actions_creation_day_array,
        backgroundColor: "#9933cc",
        borderColor: "#8010a0"
      },
      {
        label: t("stats.labels.completed"),
        data: @actions_completion_day_array,
        backgroundColor: "#0066cc",
        borderColor: "#0066cc"
      }
    ]
  },
  options: {
    title: {
      display: true,
      text: t("stats.actions_dow_30days_title")
    },
    legend: {
      labels: {
        boxWidth: 10
      }
    },
    scales: {
      xAxes: [{
        scaleLabel: {
          display: true,
          labelString: t("stats.actions_dow_30days_legend.day_of_week")
        }
      }],
      yAxes: [{
        ticks: {
          min: 0,
          max: @max + 1
        },
        scaleLabel: {
          display: true,
          labelString: t("stats.actions_dow_30days_legend.number_of_actions")
        }
      }]
    },
    responsive: false
  }
}.to_json
%>
