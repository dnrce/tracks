<%==
{
  type: "bar",
  data: {
    labels: array_of_month_labels(@done_count_array.size),
    datasets: [
      {
        label: t("stats.labels.created"),
        data: @actions_created_last12months_array,
        backgroundColor: "#9933cc",
        borderColor: "#8010a0"
      },
      {
        label: t("stats.labels.completed"),
        data: @actions_done_last12months_array,
        backgroundColor: "#0066cc",
        borderColor: "#0066cc"
      },
      {
        type: "line",
        label: t("stats.labels.avg_created"),
        data: @created_count_array,
        borderColor: "#00ff00",
        borderWidth: 3,
        pointRadius: 0
      },
      {
        type: "line",
        label: t("stats.labels.avg_completed"),
        data: @done_count_array,
        borderColor: "#ff0000",
        borderWidth: 3,
        pointRadius: 0
      },
      {
        type: "line",
        label: t("stats.labels.month_avg_created", months: 3),
        data: @actions_created_avg_last12months_array,
        borderColor: "#007700",
        borderWidth: 3,
        pointRadius: 0
      },
      {
        type: "line",
        label: t("stats.labels.month_avg_completed", months: 3),
        data: @actions_done_avg_last12months_array,
        borderColor: "#aa0000",
        borderWidth: 3,
        pointRadius: 0
      }
    ]
  },
  options: {
    title: {
      display: true,
      text: t("stats.actions_lastyear_title")
    },
    legend: {
      labels: {
        boxWidth: 10
      }
    },
    scales: {
      xAxes: [{
        scaleLabel: {
          display: true,
          labelString: t("stats.legend.months_ago")
        }
      }],
      yAxes: [{
        ticks: {
          min: 0,
          max: @max + @max/10 + 1
        },
        scaleLabel: {
          display: true,
          labelString: t("stats.legend.number_of_actions")
        }
      }]
    },
    responsive: false
  }
}.to_json
%>
